1. Создать классы объектов.
- Родительский ->
- Точка (x, y)
- Прямоугольник (x1, y1, x2, y2)
- Линия (x1, x2, y1, y2) // Была опечатка в тз
- Круг (x, y, r)

2.? Создать интерфейс для вывода записи (Тип обьекта и его параметры: координаты). // Опционально, для избавления от зависимостей.

3. Реализовать Фабрику (порождающий паттерн), для создания новых объектов.

4. Создать модуль (класс) для отбработки входящего файла в текстовом формате (.тхт).
На основе обработанных данных, фабрика должна создавать нужные объекты.

5. Реализовать метод Intersect() для определения наличия пересечения двух объектов, по координатам.
Если пересечений нет - вернёт сообщение об их отсутствии.

6.? Создать меню для работы с программой.